<h1><%=user.name%></h1>

<h1>投稿フォーム</h1>
<%= form_with model: book, local:true do |f| %>
  <h4>本のタイトル</h4>
  <%= f.text_field :title %>
  <h4>感想文</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>
<% end %>