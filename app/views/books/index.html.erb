<h3>books_index</h3>
<% @books.each do |book| %>
  <p>本の題名：
    <%= link_to book_path(book.id) do %>
      <%= book.title %>
    <% end %>
  </p>
  <p><%= user.name %></p>
  
  <p>説明：<%= book.body %></p>
  <p>ユーザーネーム：<%= book.user.name %></p>
<% end %>
<h3>books_index</h3>